select empno, ename, sal from emp 
where ename like '%A%'

select empno, ename, sal from emp 
where ename like '%A'

select empno, ename, sal from emp 
where ename like '_S_G'

select ename from emp
where regexp_like(ename,'A')
order by ename;
